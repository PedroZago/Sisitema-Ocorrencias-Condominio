import { Routes } from '@angular/router';

import { UnidadesComponent } from './components';

export const UnidadesRoutes: Routes = [
    {
        path: 'unidades',
        component: UnidadesComponent
    }
];